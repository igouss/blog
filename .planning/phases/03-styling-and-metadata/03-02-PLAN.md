---
phase: 03-styling-and-metadata
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [STYL-01, STYL-02, STYL-03, META-01]

must_haves:
  truths:
    - "Article pages are visually styled with SARA warm typography — not raw browser defaults"
    - "A visible '← Back' link appears above the article title"
    - "Pages are usable on mobile viewport — no horizontal scroll, readable text"
    - "og:title meta tag contains the article's title from front matter"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 3 success criteria against the live public URL. Claude runs automated curl checks first; human confirms visual criteria (typography, mobile, navigation) by loading the article URL in a browser.

Purpose: Confirm Phase 3 styling and metadata work as deployed — not just in source files.
Output: Human approval that all 4 success criteria are TRUE.
</objective>

<execution_context>
@/home/elendal/.claude/get-shit-done/workflows/execute-plan.md
@/home/elendal/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-styling-and-metadata/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated verification checks</name>
  <files></files>
  <action>
Run the following curl checks against the live article URL. Read the public hostname from configs/Caddyfile or .planning/STATE.md. The article URL should be a real article that exists (e.g., the one used in Phase 2 verification). Run all checks and report results:

1. **OG title check (success criterion 4):**
   ```bash
   curl -s https://<hostname>/articles/<slug> | grep "og:title"
   ```
   Expected: line containing `property="og:title"` with a non-empty content attribute.

2. **Back nav check (success criterion 2):**
   ```bash
   curl -s https://<hostname>/articles/<slug> | grep "back-nav"
   ```
   Expected: `<a href="/" class="back-nav">← Back</a>`

3. **Nunito font loaded:**
   ```bash
   curl -s https://<hostname>/articles/<slug> | grep "Nunito"
   ```
   Expected: Google Fonts link with Nunito.

4. **HTTP 200 (no template errors):**
   ```bash
   curl -s -o /dev/null -w "%{http_code}" https://<hostname>/articles/<slug>
   ```
   Expected: 200

5. **CSS file served (not 404):**
   ```bash
   curl -s -o /dev/null -w "%{http_code}" https://<hostname>/css/blog.css
   ```
   Expected: 200

Report each check result clearly. If any check fails, diagnose before presenting the human checkpoint.
  </action>
  <verify>All 5 curl checks return expected results. If any fail, fix the issue (template error, Caddy not reloaded, file not deployed to /srv/blog) before proceeding to the checkpoint.</verify>
  <done>All automated checks pass: og:title populated, back-nav present, Nunito loaded, HTTP 200 on article, HTTP 200 on CSS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — typography, navigation, mobile, dark mode</name>
  <what-built>
Phase 3 styling and metadata: css/blog.css (SARA warm design language, dark/light mode toggle, mobile responsive) + templates/article.html (Nunito font, og:title meta, ← Back nav, theme toggle button).
  </what-built>
  <how-to-verify>
Open the article URL in a browser (use a real article from Phase 2 verification). Verify:

1. **Typography (STYL-01):** Page shows warm cream background, readable Nunito font, generous spacing — NOT bare browser default styling (Times New Roman on white).

2. **Navigation (STYL-02):** A "← Back" link is visible above the article title, links to /.

3. **Mobile (STYL-03):** Resize the browser window to ~375px wide. Page remains usable: text is readable (18px), no horizontal scrollbar appears.

4. **Dark mode:** Click the ◐/◑ button (top-right corner). Page switches between light and dark themes. Reload the page — theme persists.

5. **OG tag (META-01):** Already verified by curl. Optionally: View page source and find `og:title` with the article's title.
  </how-to-verify>
  <resume-signal>Type "approved" if all criteria pass, or describe any issues to fix.</resume-signal>
  <action>Human visual verification of Phase 3 styling in browser — see how-to-verify steps above.</action>
  <verify>Human confirms all 4 Phase 3 success criteria are met in the live browser.</verify>
  <done>Human types "approved" — all success criteria visually confirmed on public URL.</done>
</task>

</tasks>

<verification>
Phase 3 is complete when:
1. `curl -s <article-url> | grep og:title` returns a populated og:title tag
2. `curl -s <article-url> | grep back-nav` returns the back navigation anchor
3. `curl -s -o /dev/null -w "%{http_code}" <article-url>` returns 200
4. Human confirms: SARA styling visible (no raw browser defaults), ← Back link visible, mobile viewport usable
</verification>

<success_criteria>
All 4 Phase 3 success criteria confirmed TRUE:
1. Article pages render with readable SARA typography (not raw browser defaults)
2. Every article page has a visible ← Back navigation link
3. Blog layout usable on mobile viewport (no horizontal scroll, readable font size)
4. curl -s <article-url> | grep og:title returns populated og:title from front matter
Human approved at checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/03-styling-and-metadata/03-02-SUMMARY.md`
</output>
